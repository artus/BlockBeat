export class Person
{
    constructor(public id : string, public userName : string, public firstName : string, public lastName : string, public password : string)
    {
        // Let typescript handle the rest.
    }
}